# Инструкция по развертыванию слотов для мини-аппа

## 📋 Что было сделано

1. ✅ Создана HTML страница слотов с игровым полем и возможными призами
2. ✅ Добавлены CSS стили для анимации прокрутки с размытием
3. ✅ Реализована JavaScript логика для работы со слотами
4. ✅ Доработан API сервер для обработки запросов слотов

## 🚀 Что нужно сделать для развертывания

### Шаг 1: Загрузить файлы на сервер (API)

**На локальном компьютере (PowerShell):**

```powershell
cd "F:\ytouy\Documents\казик мини аппп\тг каз для 11"

# Загрузить обновленный api_server.py
scp api_server.py root@141.8.198.144:/opt/telegram_bot/
```

**На сервере:**

```bash
ssh root@141.8.198.144
cd /opt/telegram_bot

# Остановить бота
systemctl stop telegram-bot

# Проверить синтаксис
source venv/bin/activate
python3 -m py_compile api_server.py

# Запустить бота
systemctl start telegram-bot

# Проверить статус
systemctl status telegram-bot

# Просмотреть логи
journalctl -u telegram-bot -f
```

### Шаг 2: Загрузить файлы мини-аппа на Netlify

**Вариант A: Через Netlify CLI (если установлен)**

```bash
cd "F:\ytouy\Documents\казик мини аппп\тг каз для 11\mini_app"
netlify deploy --prod
```

**Вариант B: Через Git (если используется Git)**

1. Закоммитьте изменения:
```bash
cd "F:\ytouy\Documents\казик мини аппп\тг каз для 11"
git add mini_app/
git commit -m "Добавлены слоты для мини-аппа"
git push
```

2. Netlify автоматически задеплоит изменения

**Вариант C: Через веб-интерфейс Netlify**

1. Откройте https://app.netlify.com
2. Выберите проект `arbuzcas`
3. Перейдите в "Deploys"
4. Нажмите "Trigger deploy" → "Deploy site"
5. Или загрузите файлы через "Site settings" → "Build & deploy" → "Deploy manually"

**Вариант D: Через drag & drop**

1. Откройте https://app.netlify.com/drop
2. Перетащите папку `mini_app` (со всеми файлами)
3. Дождитесь завершения деплоя

### Шаг 3: Проверить пути к assets

Убедитесь, что папка `assets` доступна для мини-аппа. 

**Если assets находится на уровне выше mini_app:**

Текущие пути в коде правильные: `../assets/`

**Если нужно скопировать assets в mini_app:**

```bash
# На локальном компьютере
cd "F:\ytouy\Documents\казик мини аппп\тг каз для 11"
xcopy assets mini_app\assets\ /E /I
```

Затем обновите пути в `app.js`:
- Замените `../assets/` на `assets/`

### Шаг 4: Настроить API_BASE в app.js

Откройте `mini_app/app.js` и проверьте настройку `API_BASE`:

```javascript
// Для продакшена на Netlify
if (window.location.hostname !== 'localhost' && window.location.hostname !== '127.0.0.1') {
    // ЗАМЕНИТЕ НА ВАШ РЕАЛЬНЫЙ URL API СЕРВЕРА!
    API_BASE = 'https://your-server.com:8080/api';
    // Или если используете прокси (nginx):
    // API_BASE = 'https://api.your-domain.com/api';
}
```

**Важно:** Замените `your-server.com` на реальный IP или домен вашего сервера.

### Шаг 5: Проверить работу

1. **Откройте мини-апп в Telegram:**
   - Найдите вашего бота
   - Откройте мини-апп
   - Перейдите в "Игры" → "Слоты"

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку "Console"
   - Проверьте наличие ошибок

3. **Проверьте логи сервера:**
   ```bash
   ssh root@141.8.198.144
   journalctl -u telegram-bot -f
   ```

## 📁 Структура файлов для деплоя

### На сервере (API):
```
/opt/telegram_bot/
├── api_server.py          ← ОБНОВЛЕН
├── main.py
├── handlers/
│   └── games.py
├── utils/
│   └── checks.py
└── ...
```

### На Netlify (мини-апп):
```
mini_app/
├── index.html             ← ОБНОВЛЕН
├── app.js                 ← ОБНОВЛЕН
├── styles.css             ← ОБНОВЛЕН
├── assets/                ← НОВАЯ ПАПКА (если копируете)
│   ├── seven.svg
│   ├── grape.svg
│   ├── lemon.svg
│   ├── bar.svg
│   └── dollar-svgrepo-com.svg
└── ...
```

## 🔧 Решение проблем

### Проблема: Слоты не запускаются

1. **Проверьте логи API:**
   ```bash
   journalctl -u telegram-bot | grep -i slot
   ```

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Проверьте ошибки в Console

3. **Проверьте API_BASE:**
   - Убедитесь, что URL правильный
   - Проверьте доступность API: `https://your-api-url/api/user`

### Проблема: Анимация не работает

1. **Проверьте пути к SVG:**
   - Убедитесь, что файлы `seven.svg`, `grape.svg`, `lemon.svg`, `bar.svg` доступны
   - Проверьте пути в коде (`../assets/` или `assets/`)

2. **Проверьте консоль браузера:**
   - Ищите ошибки загрузки изображений

### Проблема: Результат не отображается

1. **Проверьте логи API:**
   ```bash
   journalctl -u telegram-bot | grep -i "mini_app_slots"
   ```

2. **Проверьте, что dice сообщение отправляется:**
   - Откройте чат с ботом
   - Должно появиться сообщение со слотом

## ✅ Чеклист развертывания

- [ ] Загружен `api_server.py` на сервер
- [ ] Бот перезапущен на сервере
- [ ] Загружены файлы мини-аппа на Netlify
- [ ] Настроен `API_BASE` в `app.js`
- [ ] Проверены пути к assets (SVG файлы)
- [ ] Протестирована работа слотов в мини-аппе
- [ ] Проверены логи на наличие ошибок

## 📞 Полезные команды

### На сервере:
```bash
# Перезапуск бота
systemctl restart telegram-bot

# Просмотр логов
journalctl -u telegram-bot -f

# Проверка статуса
systemctl status telegram-bot
```

### На локальном компьютере:
```powershell
# Загрузка файла на сервер
scp api_server.py root@141.8.198.144:/opt/telegram_bot/

# Загрузка папки на сервер
scp -r mini_app/* root@141.8.198.144:/opt/telegram_bot/mini_app/
```

---

**Последнее обновление:** Декабрь 2024

