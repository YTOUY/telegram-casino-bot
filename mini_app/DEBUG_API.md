# Диагностика ошибки подключения к API

## Что было исправлено

1. ✅ Улучшена Netlify Function (`netlify/functions/api-proxy.js`)
   - Правильная обработка путей
   - Улучшенная передача заголовков
   - Добавлено логирование
   - Добавлен таймаут 30 секунд

2. ✅ Улучшена функция `getInitData()` в `app.js`
   - Правильное получение initData из Telegram WebApp API
   - Формирование initData из initDataUnsafe при необходимости

3. ✅ Улучшена обработка ошибок в `loadUserData()`
   - Детальное логирование всех запросов
   - Более информативные сообщения об ошибках

## Проверка работы

### 1. Проверьте логи Netlify Functions

Откройте: https://app.netlify.com/projects/arbuzcas/logs/functions

Ищите ошибки при вызове функции `api-proxy`.

### 2. Проверьте работу API на сервере

**На сервере (SSH):**
```bash
# Проверьте что тестовый бот запущен
systemctl status telegram-bot-test

# Проверьте логи
journalctl -u telegram-bot-test -n 50

# Проверьте что порт 8081 слушается
ss -tlnp | grep 8081

# Проверьте что API отвечает локально
curl http://localhost:8081/api/user
```

Должна быть ошибка 401 (Unauthorized) - это нормально, значит API работает.

### 3. Проверьте доступность API извне

**С вашего компьютера:**
```bash
curl http://141.8.198.144:8081/api/user
```

Если не работает, проверьте:
- Файрвол открыт для порта 8081: `ufw status | grep 8081`
- API сервер запущен: `systemctl status telegram-bot-test`

### 4. Проверьте консоль браузера

1. Откройте мини-апп в Telegram
2. Откройте консоль разработчика (F12 или через меню)
3. Ищите сообщения:
   - `🌐 API_BASE установлен:` - должен показать путь к Netlify Function
   - `📡 Запрос данных пользователя...` - должен показать детали запроса
   - `❌` - любые ошибки

### 5. Проверьте Netlify Function напрямую

Откройте в браузере:
```
https://arbuzcas.netlify.app/.netlify/functions/api-proxy/api/user
```

Должна быть ошибка (401 или 500), но не 404. Если 404 - функция не развернута.

## Возможные проблемы и решения

### Проблема: Netlify Function возвращает 404

**Решение:**
1. Убедитесь что файл находится в `mini_app/netlify/functions/api-proxy.js`
2. Переразверните на Netlify: `netlify deploy --prod`
3. Проверьте логи развертывания на наличие ошибок

### Проблема: API сервер недоступен

**Решение:**
1. Проверьте что тестовый бот запущен на сервере
2. Проверьте что порт 8081 открыт в файрволе
3. Проверьте логи бота на наличие ошибок

### Проблема: initData не передается

**Решение:**
1. Убедитесь что мини-апп открыт через Telegram (не напрямую в браузере)
2. Проверьте консоль браузера - должно быть сообщение о initData
3. Если initData отсутствует, проверьте настройки бота в @BotFather

### Проблема: CORS ошибки

**Решение:**
Netlify Function уже настроена с CORS заголовками. Если все равно есть ошибки:
1. Проверьте логи Netlify Function
2. Убедитесь что функция правильно передает заголовки

## Тестирование

После исправлений:

1. Откройте мини-апп в Telegram
2. Откройте консоль браузера (F12)
3. Проверьте логи:
   - Должен быть запрос к `/.netlify/functions/api-proxy/api/user`
   - Должен быть ответ от API (даже если 401)
4. Если все еще ошибка - проверьте логи Netlify Function

## Контакты

Если проблема не решена:
1. Проверьте логи Netlify Function: https://app.netlify.com/projects/arbuzcas/logs/functions
2. Проверьте логи тестового бота: `journalctl -u telegram-bot-test -n 100`
3. Проверьте консоль браузера в мини-аппе

