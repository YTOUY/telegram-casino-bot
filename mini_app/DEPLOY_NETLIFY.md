# 🚀 Деплой на Netlify

## Быстрый старт

### Вариант 1: Через Netlify CLI (рекомендуется)

1. **Установите Netlify CLI** (если еще не установлен):
```bash
npm install -g netlify-cli
```

2. **Войдите в Netlify**:
```bash
netlify login
```

3. **Перейдите в папку mini_app**:
```bash
cd "тг каз для 11/mini_app"
```

4. **Инициализируйте проект** (если еще не инициализирован):
```bash
netlify init
```
   - Выберите "Create & configure a new site"
   - Выберите команду для сборки: `echo 'No build needed'` или просто Enter
   - Выберите директорию для публикации: `.` (текущая папка)

5. **Деплой**:
```bash
netlify deploy --prod
```

### Вариант 2: Через GitHub (автоматический деплой)

1. **Создайте репозиторий на GitHub** (если еще нет):
   - Создайте новый репозиторий на GitHub
   - Загрузите код в репозиторий

2. **Подключите к Netlify**:
   - Зайдите на [netlify.com](https://netlify.com)
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите ваш GitHub репозиторий
   - Настройки:
     - **Base directory**: `тг каз для 11/mini_app` (или оставьте пустым, если репозиторий содержит только mini_app)
     - **Build command**: `echo 'No build needed'` (или оставьте пустым)
     - **Publish directory**: `.` (или оставьте пустым)

3. **Нажмите "Deploy site"**

4. **После деплоя**:
   - Netlify создаст URL вида `https://your-site-name.netlify.app`
   - Обновите URL в настройках бота (`handlers/mini_app.py`)

### Вариант 3: Через Drag & Drop

1. **Соберите файлы для деплоя**:
   - Убедитесь, что все файлы находятся в папке `mini_app/`
   - Убедитесь, что папка `nft/` скопирована (если используется)

2. **Загрузите на Netlify**:
   - Зайдите на [app.netlify.com/drop](https://app.netlify.com/drop)
   - Перетащите папку `mini_app/` в браузер
   - Дождитесь завершения деплоя

## ⚙️ Конфигурация

### Файл `netlify.toml`

Уже настроен в проекте:
```toml
[build]
  publish = "."
  command = "echo 'No build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  port = 8888
  publish = "."
  functions = "netlify/functions"
```

### Переменные окружения (если нужны)

В настройках сайта на Netlify можно добавить переменные окружения:
- `API_URL` - URL вашего API сервера (если нужно)

## 🔧 Настройка API

### Текущая конфигурация

API проксируется через Netlify Function (`netlify/functions/api-proxy.js`).

**Текущий API URL**: `http://141.8.198.144:8081`

Если нужно изменить URL API сервера, отредактируйте файл `netlify/functions/api-proxy.js`:
```javascript
const apiUrl = `http://YOUR_API_SERVER:PORT${path}`;
```

### Проверка работы API

После деплоя проверьте:
1. Откройте `https://your-site.netlify.app/.netlify/functions/api-proxy/api/user`
2. Должна быть ошибка авторизации (не 404), что означает, что прокси работает

## 📱 Десктопная версия Telegram Mini App

### Как включить десктопную версию

Telegram Mini Apps работают в десктопной версии Telegram, но требуют специальных настроек:

1. **Установите Telegram Desktop Beta**:
   - Скачайте с [telegram.org/desktop/download](https://telegram.org/desktop/download)
   - Установите Beta версию

2. **Включите WebView Inspection**:
   - Откройте Telegram Desktop Beta
   - Меню (три линии) → Settings → Advanced
   - Experimental settings → Enable webview inspecting

3. **Тестирование**:
   - Мини-апп автоматически адаптируется под размер окна
   - Десктопная версия использует медиа-запрос `@media (min-width: 768px)`
   - Горизонтальная компоновка активируется автоматически

### Особенности десктопной версии

- ✅ Горизонтальная компоновка (слот-машина слева, призы справа)
- ✅ Увеличенные элементы для лучшей видимости
- ✅ Оптимизированная навигация для мыши и клавиатуры
- ✅ Автоматическое определение устройства через JavaScript

## 🔍 Проверка деплоя

После деплоя проверьте:

1. **Откройте сайт в браузере**:
   - `https://your-site.netlify.app`
   - Должна открыться страница мини-аппа

2. **Проверьте консоль браузера** (F12):
   - Не должно быть критических ошибок
   - Проверьте подключение к API

3. **Проверьте в Telegram**:
   - Откройте бота
   - Нажмите кнопку для открытия мини-аппа
   - Мини-апп должен открыться внутри Telegram

## 🐛 Отладка

### Проблемы с API

Если API не работает:
1. Проверьте URL в `netlify/functions/api-proxy.js`
2. Проверьте, что API сервер доступен из интернета
3. Проверьте логи Netlify Functions в панели Netlify

### Проблемы с деплоем

Если деплой не работает:
1. Проверьте, что все файлы в папке `mini_app/`
2. Проверьте `netlify.toml` на наличие ошибок
3. Проверьте логи в панели Netlify

### Проблемы с CORS

Если возникают ошибки CORS:
- Netlify Function уже настроена для CORS
- Проверьте заголовки в `netlify/functions/api-proxy.js`

## 📝 Обновление деплоя

### Через CLI:
```bash
cd "тг каз для 11/mini_app"
netlify deploy --prod
```

### Через GitHub:
- Просто сделайте `git push` - Netlify автоматически задеплоит изменения

### Через Drag & Drop:
- Просто перетащите обновленную папку на [app.netlify.com/drop](https://app.netlify.com/drop)

## 🔗 Полезные ссылки

- [Netlify Dashboard](https://app.netlify.com)
- [Netlify CLI Documentation](https://docs.netlify.com/cli/get-started/)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Desktop Beta](https://telegram.org/desktop/download)

## ✅ Чеклист перед деплоем

- [ ] Все файлы находятся в папке `mini_app/`
- [ ] `netlify.toml` настроен правильно
- [ ] `netlify/functions/api-proxy.js` содержит правильный URL API
- [ ] `_headers` файл настроен (если нужен)
- [ ] Проверена работа локально через `netlify dev`
- [ ] URL мини-аппа обновлен в настройках бота
