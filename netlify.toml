[build]
  publish = "mini_app"
  command = "echo 'No build needed'"

# Netlify автоматически обрабатывает функции в /.netlify/functions/
# Все остальные запросы перенаправляем на index.html для SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "echo 'Development server'"
  port = 8888

